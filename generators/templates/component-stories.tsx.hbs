import { Story, Meta } from '@storybook/react/types-6-0';

import { {{pascalCase name}}, {{pascalCase name}}Props } from './{{pascalCase name}}';

const story: Meta<{{pascalCase name}}Props> = {
  title: 'Atoms/{{pascalCase name}}',
  component: {{pascalCase name}},
  args: {},
  argTypes: {},
  parameters: {}
} as Meta;

export const {{pascalCase name}}Default: Story<{{pascalCase name}}Props> = args => (
  <{{pascalCase name}} {...args} />
);

export default story;
