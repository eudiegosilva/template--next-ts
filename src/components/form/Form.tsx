import {
  FormLogin,
  type FormLoginProps,
  type FormLoginDataProps
} from './form-login/FormLogin';

export type FormProps = {};

const Form = {
  FormLogin
};

export { Form, type FormLoginProps, type FormLoginDataProps };
